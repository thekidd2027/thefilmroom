"use client";
import {useEffect,useState} from "react";
export default function SetupPage(){const [d,setD]=useState<any>(null);useEffect(()=>{fetch('/api/setup/status').then(r=>r.json()).then(setD)},[]);return <div className="p-8 max-w-3xl"><h1 className="font-display text-3xl mb-2">SETUP</h1><p className="text-dim text-sm mb-6">All green means the portal has the credentials it needs. Values are never displayed.</p><div className="space-y-2">{d?.services?.map((s:any)=><div key={s.name} className="panel p-3 flex justify-between"><span className="font-mono text-sm">{s.name}</span><span className={s.configured?'text-go':'text-signal'}>{s.configured?'Configured':'Missing'}</span></div>)}</div><div className="panel p-4 mt-6 text-sm text-dim"><b className="text-paper">Required services:</b> Supabase stores users/jobs/videos; OpenAI researches trends and makes editorial decisions; YouTube Data API discovers source videos and engagement; Supadata supplies timestamped transcripts and visual video inspection so timestamps/angles are grounded.</div></div>}
